server:
  port: 8761

spring:
  application:
    name: hello-sc-alibaba-config

  cloud:
    nacos:
      discovery:
        server-addr: 10.0.99.142:8848
        namespace: 997dfea3-5cb8-4300-b286-5d236172a819
      config:
        refresh-enabled: true  #该版本默认为true
        #file-extension: yaml  #该配置添加yaml配置文件支持, 添加后就只支持yaml后缀的配置
author:
  name: 高斯林